arch = [
    "niri",
    "qt5-wayland",
    "kitty",
    "uwsm",
    "wl-clipboard",
    "xdg-desktop-portal",
    "xdg-desktop-portal-gtk",
    "gnome-keyring",
    "adwaita-qt5-git",
    "adwaita-qt6-git",
    "swaylock",

    # services
    { package = "polkit-kde-agent", hooks = { after_install = ["systemctl", "--user", "add-wants", "niri.service", "plasma-polkit-agent.service"] } },
    { package = "xwayland-satellite", hooks = { after_install = ["systemctl", "--user", "add-wants", "niri.service", "xwayland-satellite.service"] } },
    { package = "swww", hooks = { after_install = ["systemctl", "--user", "add-wants", "niri.service", "swww.service"] } },
    { package = "swaync", hooks = { after_install = ["systemctl", "--user", "add-wants", "niri.service", "swaync.service"] } },
    { package = "swayidle", hooks = { after_install = ["systemctl", "--user", "add-wants", "niri.service", "swayidle.service"] } },

    # fonts
    "noto-fonts",
    "noto-fonts-cjk",
    "noto-fonts-emoji",
    "ttf-jetbrains-mono",

    # shell
    "rofi-wayland",
    "rofi-calc",

    # shell: eww compilation dependencies
    # we are compiling eww from scratch
    "gtk3",
    "gtk-layer-shell",
    "pango",
    "gdk-pixbuf2",
    "libdbusmenu-gtk3",
    "cairo",
    "glib2",
    "gcc-libs",
    "glibc",
]
